public-hoist-pattern[]=eslint*
public-hoist-pattern[]=eslint-config-*

# React Native projects pull react-dom via expo-router -> @radix-ui/* for web
# support, but react-dom peer-deps on a newer react than our RN pin.
# Safe to ignore — react-dom is not used at runtime in RN apps.
strict-peer-dependencies=false
