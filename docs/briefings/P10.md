# P10: Simple Chat UI Component 💬

deps:P9 | verify:`npm run typecheck -w ui && npm run test -w ui`

**Goal:** Clean streaming chat UI using direct n9router completions. Model name as sender. No reasoning bleed.

| # | Task | File | Change | ⏱ | Status |
|---|------|------|--------|----|--------|
| 10.1 | Create `N9RouterChatClient` | `ui/src/services/n9routerChat.ts` (NEW) | Class with `chatCompletion()` — POST to Pilot `/api/chat/completions`, return SSE reader | 30m | ✅ |
| 10.2 | Create `SimpleChat.tsx` page | `ui/src/pages/SimpleChat.tsx` (NEW) | Clean chat UI: bubbles, streaming, typing indicator, model name header | 1.5h | ✅ |
| 10.3 | Create `ChatMessage.tsx` component | `ui/src/components/ChatMessage.tsx` (NEW) | Single bubble: avatar, sender name, content, timestamp | 20m | ✅ |
| 10.4 | Add SimpleChat route | `ui/src/App.tsx` | Route `/chat` → `SimpleChat`. Keep `/session/:sessionId` → `Chat` | 10m | ✅ |
| 10.5 | Update navigation | `ui/src/components/Layout.tsx` | Add "Chat" and "Session" nav items | 20m | ✅ |
| 10.6 | Create `useChatStream` hook | `ui/src/services/useChatStream.ts` (NEW) | SSE reader lifecycle: start, cancel, error handling, debug entries | 30m | ✅ |
| 10.7 | Message persistence | `ui/src/pages/SimpleChat.tsx` | Save/load messages to localStorage | 20m | ✅ |
| 10.8 | Error handling in chat UI | `ui/src/pages/SimpleChat.tsx` | Error banner with retry button, error classifier | 15m | ✅ |

**Effort:** Total ~4h. Items 10.1-10.4 core (can parallel 10.1+10.2+10.3). 10.5-10.8 polish.

See [main plan](../test-overhaul-plan.md#phase-10-simple-chat-ui-component-) for full context.
