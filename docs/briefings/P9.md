# P9: Direct n9router Chat Endpoint 🚀

deps:P8 | verify:`npm run typecheck -w server && npm run test -w server`

**Goal:** Add `POST /api/chat/completions` to Pilot server that calls n9router `/v1/chat/completions` directly, bypassing OpenCode session agent protocol.

| # | Task | File | Change | ⏱ | Status |
|---|------|------|--------|----|--------|
| 9.1 | Create `n9routerChat.ts` server module | `server/src/n9routerChat.ts` (NEW) | Export `setupChatRouter()` — reads request body, forwards to n9router with `stream: true`, pipes SSE response | 30m | ✅ |
| 9.2 | Register chat route in `index.ts` | `server/src/index.ts` | Call `setupChatRouter()` in `startServer()`, pass n9router URL + API key from env | 10m | ✅ |
| 9.3 | Export `setupChatRouter()` + env wiring | `server/src/n9routerChat.ts` | Read `N9ROUTER_URL` and `N9ROUTER_API_KEY` from env, create Hono route, handle errors | 15m | ✅ |
| 9.4 | Add env vars to server config | `server/src/cli.ts` | Document `N9ROUTER_URL` and `N9ROUTER_API_KEY` in help text | 5m | ✅ |
| 9.5 | Add error handling for n9router failures | `server/src/n9routerChat.ts` | Catch fetch errors, n9router error responses (401/402/429/503), return clear JSON | 15m | ✅ |
| 9.6 | Add request logging | `server/src/n9routerChat.ts` | Log model, token counts, latency on completion | 10m | ✅ |

**Effort:** Total ~1.5h. Items 9.1-9.3 core, 9.4-9.6 polish.

**API Contract:**
```
POST /api/chat/completions
Authorization: Bearer <pilot-auth-token>
Content-Type: application/json
{ "messages": [{"role":"user","content":"Build timer"}], "model":"ds/deepseek-v4-flash" }

Response: SSE stream (text/event-stream)
data: {"choices":[{"delta":{"content":"Here"},"index":0}]}
```

See [main plan](../test-overhaul-plan.md#phase-9-direct-n9router-chat-endpoint-) for full context.
