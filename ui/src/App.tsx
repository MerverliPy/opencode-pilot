/**
 * Pilot UI — Root application component.
 *
 * M2 will add routing, layout, and all screen components.
 * This is a minimal scaffold that confirms the app boots.
 */
export function App() {
  return (
    <div style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>✈ Pilot</h1>
      <p>OpenCode web client — scaffolded and ready for M2.</p>
    </div>
  );
}
